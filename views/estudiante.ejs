<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Resumen estudiante</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container mt-5">
  <a href="/dashboard" class="btn btn-secondary mb-3">← Volver</a>

  <h3>📄 Progreso de <%= estudiante.nombreestudiante %></h3>
  <p>Código: <strong><%= estudiante.codigoestudiante %></strong></p>
  <p>Grupo: <strong><%= estudiante.grupo %></strong></p>

  <table class="table table-bordered">
    <thead>
      <tr>
        <th>Pregunta</th>
        <th>Respuesta</th>
        <th>¿Correcta?</th>
        <th>Feedback</th>
      </tr>
    </thead>
    <tbody>
    <% respuestas.forEach(r => { %>
      <tr>
        <td>#<%= r.idpregunta %></td>
        <td><%= r.respuesta %></td>
        <td><%= r.correcta ? "✔️" : "❌" %></td>
        <td><%= r.feedback || "-" %></td>
      </tr>
    <% }); %>
    </tbody>
  </table>
</div>
</body>
</html>
